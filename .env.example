# Generate both with: openssl rand -hex 32   (or just run ./start.sh)

# Encryption key for stored secrets (libsodium secretbox).
SECRETS_KEY=

# Shared bearer token every client sends. Leave empty to disable auth (dev only).
API_TOKEN=
